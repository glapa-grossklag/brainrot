[
  Print out all non-zero bytes, 1 to 255.

  This is an example of a comment in which you can use reserved syntax (- + < >
  , . [ ]) without worry. This is because cell 0 is always 0 at the start of
  the program, meaning everything inside of this "loop" is skipped.
]

+ [ .+ ]
